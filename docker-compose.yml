version: '3'
services:
    cef-container:
        build:
            context: .
        container_name: cef-container
        ports:
            - 5000:22
        volumes:
            - ../../:/workspace:cached
        deploy:
            resources:
                reservations:
                    memory: 1024M
                limits:
                    memory: 4000M
        #command: /bin/bash -c "cd /workspace && while true; do sleep 3600; done"